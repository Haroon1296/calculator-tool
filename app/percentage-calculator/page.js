import PercentageCalculatorClient from "./PercentageCalculatorClient";

export const metadata = {
  title: "Percentage Calculator | Percentages, Ratios, and Change",
  description:
    "Solve percentage of a value, percentage ratio, and percentage change with a clean, modern percentage calculator."
};

export default function PercentageCalculatorPage() {
  return <PercentageCalculatorClient />;
}
